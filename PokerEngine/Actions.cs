using Poker.Model;
using Poker.Model.Enums;
using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace Poker.Engine
{
    public static class GameAction
    {
        //public static IGame StartGame()
        //{
        //    return new Game();
        //}

        //private static Action InitializeDeck(IGame game)
        //{
            
        //    foreach (var s in Enum.GetValues(typeof(eSuit)).Cast<eSuit>())
        //    {
        //        foreach (var r in Enum.GetValues(typeof(eRank)).Cast<eRank>())
        //        {
        //            game.Deck.Add(new Card(s, r));
        //        }
        //    }
        //    return game;
        //}
    }


}
